# Muthur Command Backend machine image — Neardi Pi 3，aarch64，基于与 arch-mc 一致的运行时。
ARG BUILD_FROM=ghcr.io/muthur-command/aarch64-muthurcommand:latest
FROM ${BUILD_FROM}

# 按需增加板级依赖（GPIO、编解码等）：
# RUN apt-get update && apt-get install -y --no-install-recommends ...

LABEL io.mcio.machine="neardi-pi-3"
LABEL org.opencontainers.image.description="Muthur Command Backend for neardi-pi-3"
